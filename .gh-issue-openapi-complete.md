## Summary
Complete OpenAPI specification by documenting all existing API v1 endpoints with proper schemas, examples, and comprehensive documentation.

## Current Status
- Basic OpenAPI spec exists in `backend/openapi.yaml`
- Error schemas added (#113)
- Security schemes configured
- Many v1 endpoints not yet documented

## Tasks
- [ ] Complete inventory of all Express routes in backend/routes/v1/
- [ ] Document all `/api/v1/auth/*` endpoints (11 endpoints)
- [ ] Document all `/api/v1/activities/*` endpoints (5 endpoints)
- [ ] Document all `/api/v1/health-metrics/*` endpoints (5 endpoints)
- [ ] Add request/response examples for each endpoint
- [ ] Add parameter descriptions and constraints
- [ ] Ensure shared schemas are reused (no duplication)
- [ ] Validate spec renders correctly in Swagger UI
- [ ] Add tags for better organization

## Acceptance Criteria
- All v1 endpoints documented in OpenAPI spec
- Each endpoint has at least one example
- Shared schemas used consistently
- Spec validates without errors
- All endpoints visible and testable in Swagger UI at `/api/docs`

## Implementation Notes
File: `backend/openapi.yaml`
Current documented endpoints: /healthz, /signup, /login, /api/activitylog, /api/healthmetrics, /api/admin/analytics

Missing documentation:
- /api/v1/auth/* (11 endpoints)
- /api/v1/activities/* (5 endpoints)  
- /api/v1/health-metrics/* (5 endpoints)
- /api/v1/health

## Related Issues
- Builds on #61 (API v1 structure)
- Builds on #113 (Error standardization)
- Prerequisite for #116 (Spectral linting)

## Epic
#45: Adventure Platform MVP Foundation

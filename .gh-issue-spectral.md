## Summary
Add Spectral linting for OpenAPI specification with CI integration to enforce consistent API design standards and catch errors automatically.

## Current Status
- OpenAPI spec in `backend/openapi.yaml`
- No automated linting or validation
- Manual review only

## Tasks
- [ ] Install Spectral as dev dependency
- [ ] Create `.spectral.yaml` config file with ruleset
- [ ] Choose ruleset: Start with spectral:oas for OpenAPI best practices
- [ ] Add custom rules for Tracksy-specific conventions
- [ ] Create npm script for local linting: `npm run lint:api`
- [ ] Add GitHub Actions workflow for CI
- [ ] Configure workflow to run on changes to openapi.yaml
- [ ] Add status badge to README
- [ ] Document linting process in CONTRIBUTING.md

## Acceptance Criteria
- Spectral installed and configured
- `npm run lint:api` validates OpenAPI spec
- GitHub Actions runs Spectral on PR and push
- CI fails on spec errors or warnings
- Status badge shows passing state
- Documentation explains how to run locally

## Implementation Notes

### Spectral Config (.spectral.yaml)
```yaml
extends: spectral:oas
rules:
  operation-operationId: error
  operation-tags: error
  operation-description: warn
  oas3-examples-value-or-externalValue: warn
```

### GitHub Actions Workflow (.github/workflows/spectral.yml)
```yaml
name: OpenAPI Lint
on:
  pull_request:
    paths:
      - 'backend/openapi.yaml'
      - '.spectral.yaml'
  push:
    branches: [main]
    paths:
      - 'backend/openapi.yaml'
      - '.spectral.yaml'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: stoplightio/spectral-action@latest
        with:
          file_glob: 'backend/openapi.yaml'
```

## Related Issues
- Depends on #116 (Complete OpenAPI paths)
- Part of API infrastructure improvements

## Epic
#45: Adventure Platform MVP Foundation
